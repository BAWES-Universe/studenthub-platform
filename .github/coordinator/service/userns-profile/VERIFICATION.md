> **REMOVED / OUT OF SCOPE — owner Option A.** This is a historical record,
> superseded by [A12-CLOSURE.md](../A12-CLOSURE.md). The static ELF runner,
> AppArmor exception profile, host installer/load/verify/teardown model,
> authenticated collector, six native production mutations and supporting
> profile design-model machinery are no longer deliverables. Production uses
> sudo → wrapper → systemd-run with `RestrictNamespaces=yes`; the namespace
> was only a test substitute for sudo/root. The inert profile and executable
> model/tests were removed. The native runner, installer and authenticated
> collector were never built; native production mutation kills were never proved.
> All requirements, commands, counts and outstanding-work language below are
> historical, not current obligations or runnable verification instructions.

# Repository verification receipt

Branch: `feat/shu251-a12-userns-profile`
Base: `00eb979800b5ef6dfb918b57002d167802238612`
Node: `v22.22.3`

`chmod -R go-w .github/coordinator` and `umask 0002` preceded the suites.

| Check | Observed result |
| --- | --- |
| Base existing focused `SHU251 contract` tests | 10 pass, 0 fail, 0 skipped |
| Final existing focused `SHU251 contract` tests | 10 pass, 0 fail, 0 skipped |
| New `userns-profile.test.mjs` | 17 pass, 0 fail, 0 skipped |
| Mutation harness | 8 individual positives pass; 8 mutants killed at named assertions |
| JS syntax checks | All four new `.mjs` files pass `node --check` |
| Protected files versus base | Both byte-identical |
| `PERMITTED_SKIPS` versus base | All eight names and reasons exactly identical |
| Actual focused-run skip names/reasons versus base | Empty before and after; no new skips |

The comparison initially used an overbroad `# SKIP` regex which also matched
Node's summary `# skipped 0`. That comparison assertion failed; the corrected
`# SKIP(?:\s|$)` pattern passed on all three saved TAP reports. No test, assertion
in the repository, or permitted skip was changed to resolve that reporting error.

Mutation output:

```text
missing-userns: positive PASS; syntax-clean mutant KILLED by A12_USERNS_REQUIRED
wrong-executable: positive PASS; syntax-clean mutant KILLED by A12_EXECUTABLE_REQUIRED
bypassed-transition: positive PASS; syntax-clean mutant KILLED by A12_TRANSITION_REQUIRED
profile-name-substitution: positive PASS; syntax-clean mutant KILLED by A12_PROFILE_NAME_REQUIRED
widened-path: positive PASS; syntax-clean mutant KILLED by A12_PATH_SCOPE_REQUIRED
global-sysctl-relaxation: positive PASS; syntax-clean mutant KILLED by A12_NO_SYSCTL_RELAXATION
ordinary-execution-exception: positive PASS; syntax-clean mutant KILLED by A12_ORDINARY_EXCLUDED
teardown-residue: positive PASS; syntax-clean mutant KILLED by A12_TEARDOWN_NO_RESIDUE
```

Exact candidate profile SHA-256:
`4e229ce4faef8e6b0303e84052b466b7dc0b297a3b6a662b53bc7cf424d6f63b`.

[receipts.example.json](receipts.example.json) contains canonical deterministic
**synthetic** install/verify/teardown receipts generated by the model, using pins
which hash `FAKE:checkout`, `FAKE:parser`, `FAKE:runner`, and `FAKE:runtime`.
They are examples of the receipt design, not evidence of installation or host
access. Every receipt carries `kind: design-simulation`.

The focused selection is intentional: the complete coordinator and L4 suites
contain real namespace/host operations forbidden by the brief. No full-suite
pass, target parser acceptance, actual AppArmor confinement, native-runner
correctness, runtime closure, successful namespace probe or actual teardown is
claimed. The supplied audit event is the source of the established causality.
The native binary and authenticated platform verifier are not implemented in
this repository-only design lane. Deployment admission remains blocked without
them; the L4 shell-wrapper namespace proof is also outside this fixed probe's
policy. These are not new skip allowances.
