# SHU-142 frontend audit — Admin app (`BAWES-Universe/studenthub-admin` @ c789b17)

Read-only audit of the Ionic/Angular admin console against the Yii2 backend `admin` app at c2ce255. All paths are relative to the respective repo root; backend paths are prefixed `admin/`, `common/` (relative to `/home/user/studenthub-c2ce255`).

## A. Identity

| Item | Value | Evidence |
|---|---|---|
| Repo / revision | `BAWES-Universe/studenthub-admin`, `c789b17` (2026-06-04, "Merge pull request #18 … fix/sentry-release-sourcemaps-admin") | `git rev-parse --short HEAD` = c789b17 |
| Stack | Angular ^15.0.0, Ionic ^6.1.9, Capacitor 4.6.1 (appId `co.studenthub.admin`), `@auth0/auth0-angular` ^1.11.1, `aws-sdk` ^2.1278 (browser S3 upload), apexcharts, ckeditor5/tinymce, `@sentry/browser` ^7.92, `@codetrix-studio/capacitor-google-auth`, `@capacitor/preferences` | `package.json:26-70`, `capacitor.config.ts:4-6` |
| API base URL (prod) | `https://admin.api.studenthub.co/v1` | `src/environments/environment.prod.ts:6` |
| Other environments | dev `https://admin.api.dev.studenthub.co/v1` (`environment.dev.ts:11`, `environment.ts:11`), docker `http://localhost:21080/v1` (`environment.docker.ts:7`), khalid/krushn local paths (`environment.khalid.ts:10`, `environment.krushn.ts:11`) | |
| Environment selection | `angular.json` `fileReplacements` per configuration: production `:70-71`, dev `:80-81`, docker `:97-98`, khalid `:114-115`, krushn `:129-130`; `defaultConfiguration: production` `angular.json:147`; npm scripts `build:production` / `build:devserver` `package.json:14-15` | |
| Backend app | Yii2 `admin` app (`admin/modules/v1/controllers/*`), URL rules in `admin/config/main.php:36-960` (`enableStrictParsing => true`, `main.php:38`) | |
| Auth transport | `Authorization: Bearer <token>` header on every authorized call, plus `Content-Type: application/json` and `Currency: <pref or KWD>` | `src/app/providers/logged-in/authhttp.service.ts:257-268` |
| Login | `GET /auth/login` with `Authorization: Basic base64(email:password)` and `g-recaptcha-response` header (`src/app/providers/auth.service.ts:288-305`); reCAPTCHA v3 site key hardcoded `src/index.html:30`, `src/app/pages/start-pages/login/login.page.ts:67`; two-step `POST /auth/login-two-step {token, otp}` (`auth.service.ts:262-278`), branch on `token_status == 0` (`login.page.ts:85-89`) | backend `admin/modules/v1/controllers/AuthController.php:135,89,262-274` |
| Federated login | Auth0 `POST /auth/login-auth0 {accessToken}` (`auth.service.ts:34,437`), domain/clientId hardcoded `src/app/app.module.ts:85-86`; Google `POST /auth/login-by-google {idToken}` (`auth.service.ts:35,373`), Google clientId hardcoded `src/app/app.component.ts:59` | `AuthController.php:195,159` |
| Session persistence | Capacitor `Preferences` key `loggedInAdmin` = JSON `{token,id,name,email,admin_limited_access}` (`auth.service.ts:243-249`; `src/app/providers/storage.service.ts:24-45`); `currency_pref`, `theme` also stored | |
| Backend auth filter | `HttpBearerAuth` on every controller except: `AwsController` (unsets authenticator and never re-adds it → `GET /aws/config` is **unauthenticated**, `admin/modules/v1/controllers/AwsController.php:15-35,56-66`); `XeroController` except `auth`,`callback` (`XeroController.php:51`); `EventController` except `text` (`EventController.php:50`) | |
| Counts | `path:` entries in router: 126 (incl. `''` redirect and `**`) — `grep -c "path:" src/app/app-routing.module.ts`; guarded by `AuthService`: 111; by `LoginGuard`: 1; services under `providers/logged-in`: 52; page components: 132 | |

## B. Screen and route map

All routes are in `src/app/app-routing.module.ts` (root, lazy-loaded); child routing modules add `:param` children (cited in the Route column). Guard `A` = `canActivate: [AuthService]` (`auth.service.ts:57-99`: only checks a stored token exists — no role/permission check). `—` = no guard. Endpoint file:line is the service call; every service goes through `authhttp.service.ts` (`get :150`, `post :180`, `patch :208`, `delete :232`, `excelget :64`, `pdfget :93`, `downloadTextFile :33`, `uploadFile :113`). Route table lookup = `routes-admin.tsv`.

| Route (`app-routing.module.ts:line`) | Screen (file) | Guard | Backend endpoints (verb path — service file:line) | Parity | Notes |
|---|---|---|---|---|---|
| `''` → `dashboard` (`:11`) | redirect | — | — | — | |
| `no-internet` (`:16`) | `pages/start-pages/no-internet` | — | none | — | static |
| `server-error` (`:23`) | `pages/errors/server-error` | — | none | — | static |
| `not-found` (`:30`) | `pages/errors/not-found` | — | none | — | static |
| `app-error` (`:437`) | `pages/errors/app-error/app-error.page.ts` | — | none (reads storage `:45`) | — | static |
| `login` (`:315`) | `pages/start-pages/login/login.page.ts` | LoginGuard (`providers/guards/login-guard.service.ts:14-21`, redirects if logged in) | GET `/auth/login` (`auth.service.ts:297-305`); POST `/auth/login-auth0` (`:437`); POST `/auth/login-by-google` (`:373`) | ID-03, ID-05 | reCAPTCHA v3 |
| `login-two-step` (`:931`, child `:token` per module) | `pages/start-pages/login-two-step/login-two-step.page.ts` | A (!) | POST `/auth/login-two-step` (`auth.service.ts:262-278`) | ID-04 | guarded by AuthService although user is not yet logged in; guard resolves false and navigates to `login` if no stored token (`auth.service.ts:90-93`) — see ADMIN-FO-40 |
| `dashboard` (`:147`) | `pages/logged-in/dashboard/dashboard.page.ts` | A | GET `/statistics?param=1&start_date&end_date` (`statistics.service.ts:23-25`); GET `/statistics/transfer?param=1…` (`:41-43`); GET `/statistics/revenue` (`:51-53`); GET `/statistics/clear-cache` (`:32-34`, page `:258`); GET `/statistics/invitation-graph-data` (`:60-62`) | RP-03, RP-04, RP-06, RP-10/OP-13, FI-19 | cache flush button reloads page (`dashboard.page.ts:256-261`) |
| `dashboard-quicks` (`:671`) | `pages/logged-in/dashboard/dashboard-quicks/dashboard-quicks.page.ts` | A | GET `/statistics?…` (`:56`), GET `/statistics/transfer?…` (`:73`) | RP-04 | "Payment Summary" |
| `stats` (`:393`) | `pages/logged-in/stats/stats.page.ts` | A | GET `/statistics/transfer?param=1` (`:37`) | RP-04 | |
| `admin-list` (`:179`) | `pages/logged-in/admin/admin-list/admin-list.page.ts` | A | GET `/admins?page` (`admin.service.ts:26`); PATCH `/admins/reset-password/{id}` (`:67`); DELETE `/admins/{id}` (`:81`) | ID-11 | limited-access badge `admin-list.page.html:29-30` |
| `admin-view/:admin_id` (`:187`; `admin-view-routing.module.ts:8`) | `admin-view.page.ts` | A | GET `/admins/{id}` (`:34`); PATCH `/admins/reset-password/{id}` (`:67`); PATCH `/admins/status-change/{id}` `{status}` (`:72-73`) | ID-11 | |
| `admin-form/:admin_id?` (`:195`; `admin-form-routing.module.ts:8`) | `admin-form.page.ts` | A | GET `/admins/{id}` (`:34`); POST `/admins` `{name,email,password,limited_access,enable_two_step_auth}` (`:43-50`); PATCH `/admins/{id}` (`:54-60`) | ID-11 | password in plaintext body (ID F5) |
| `staff-list` (`:155`) | `pages/logged-in/staff/staff-list/staff-list.page.ts` | A | GET `/staff?page&…&expand=…,permissions` (`staff.service.ts:28`); PATCH `/staff/reset-password/{id}` `{password}` (`:181-182`); PATCH `/staff/status-change/{id}` (`:186-187`); DELETE `/staff/{id}` (`:201-202`) | ID-10 | |
| `staff-view/:staff_id[/:start_date/:end_date]` (`:163`; `staff-view-routing.module.ts:8,12`) | `staff-view.page.ts` | A | GET `/staff/{id}?expand=…permissions,companies` (`:49`); PATCH `/staff/reset-password/{id}` (`:181`); PATCH `/staff/status-change/{id}` (`:186`); PATCH `/staff/recover-account/{id}` (`:191`); POST `/staff/login/{id}` (`:211`); GET `/staff/list-salaries/{id}` (`:59`); GET `/staff/list-companies/{id}` (`:39`); GET `/candidate-work-history?page…` (`candidate-work-history.service.ts:21`); GET `/daily-standup-questions/list-answers` (`daily-standup-question.service.ts:19`); GET `/requests?page…` (`request.service.ts:21`); GET `/story/list?page…` (`story.service.ts:22`); GET `/notes?…` (`note.service.ts:24`); GET `/invitations?page…` (`invitation.service.ts:21`) | ID-10, ID-17, FI-15, WK-19/21, RC-02/07, CM-16 | impersonation opens `res.redirect` in new tab `staff-view.page.ts:859` |
| `staff-form/:staff_id?` (`:171`; `staff-form-routing.module.ts:8`) | `staff-form.page.ts` | A | GET `/staff/{id}` (`:49`); POST `/staff` (`staff.service.ts:126-146`, incl. `gmail_username`, `gmail_password`, `role`, `salary`); PATCH `/staff/{id}` (`:154-173`); S3 direct upload (`aws.service.ts:185`) | ID-10, FI-15 | Gmail password sent plaintext and echoed back (ID F3) |
| `staff-salary-form/:id?` (`:471`) | `pages/logged-in/staff/staff-salary-form/staff-salary-form.page.ts` | — | GET `/staff/view-salary/{uuid}` (`staff.service.ts:69`) **MISSING-BACKEND** (rule `main.php:119` exists, no `actionViewSalary` in `StaffController.php`); POST `/staff-salary/add-salary/{staff_id}` (`staff.salary.service.ts:53-62`); PATCH `/staff-salary/update-salary/{uuid}` (`:71-80`) | FI-15 | no route guard |
| `import-salary-form` (`:475`) | `pages/logged-in/staff/import-salary-form/import-salary-form.page.ts` | — | S3 upload (`:65`); POST `/staff/import-salary {excel}` (`staff.service.ts:79-83`) | FI-15 | no route guard |
| `staff-salary-list` (`:590`) | `pages/logged-in/salary/staff-salary-list/staff-salary-list.page.ts` | A | GET `/staff-salary?page&staff_id&month` (`staff.salary.service.ts:28`); DELETE `/staff-salary/{id}` (`:43`) | FI-15 | |
| `staff-salary-register` (`:599`) | `pages/logged-in/salary/staff-salary-register/staff-salary-register.page.ts` | A | GET `/staff?page` (`staff.service.ts:28`); POST `/staff-salary/create-salary {staffIds, month}` (`staff.salary.service.ts:32-39`) | FI-15 | client keeps selected staff list `:81` |
| `staff-expense-list` (`:635`) | `pages/logged-in/staff/expenses/staff-expense-list/…page.ts` | A | GET `/staff-expenses?page&expand=staff` (`staff.expense.service.ts:28`); DELETE `/staff-expenses/{uuid}` (`:73`) | FI-14 | |
| `staff-expense-view/:expense_uuid` (`:644`) | `staff-expense-view.page.ts` | A | GET `/staff-expenses/{uuid}?expand=staff` (`:20`); PATCH `/staff-expenses/change-status/{uuid} {status}` (`:82`) | FI-14 | client state machine (FO-20) |
| `staff-leave-list` (`:662`) | `pages/logged-in/staff/staff-leaves/staff-leave-list/…page.ts` | A | GET `/staff-leave?page&expand=staff` (`staff-leave.service.ts:36`); DELETE `/staff-leave/{uuid}` (`:45`); PATCH `/staff-leave/{uuid} {status}` (`:55`) | WK-20 | |
| `staff-work-session-list` (`:581`) | `pages/logged-in/staff/staff-work-session/staff-work-session-list/…page.ts` | A | GET `/staff-work-sessions?page&expand=staff,dayActivity&…` (`staff-work-session.service.ts:30`); GET `/staff-work-sessions/list-inactive` (`:39`); GET `/staff-work-sessions/download-list-excel?download=1` (`:49-50`) | WK-19, RP-09 | client sums minutes (FO-27) |
| `daily-standup-answer-list` (`:536`) | `pages/logged-in/daily-standup/daily-standup-answer/daily-standup-answer-list/…page.ts` | A | GET `/daily-standup-answers?page&expand=staff,question&…` (`daily-standup-answer.service.ts:35`); GET `/daily-standup-answers/list-inactive` (`:44`); GET `/daily-standup-questions?page` (`daily-standup-question.service.ts:37`) | WK-21 | |
| (modal) daily-standup-answer-view | `daily-standup-answer-view.page.ts` | n/a | GET `/daily-standup-answers/{staffId}/{date}` (`:26`) | WK-21 | not routed |
| `daily-standup-question-list` (`:527`) | `daily-standup-question-list.page.ts` | A | GET `/daily-standup-questions?page` (`:37`); DELETE `/daily-standup-questions/{uuid}` (`:75`) | WK-21 | |
| `daily-standup-question-form/:question_uuid?` (`:545`) | `daily-standup-question-form.page.ts` | A | GET `/daily-standup-questions/{uuid}` (`:28`); POST `/daily-standup-questions` (`:52`); PATCH `/daily-standup-questions/{uuid}` (`:61-66`) | WK-21 | limited-access redirect (FO-02) |
| `inspector-list` (`:353`) | `pages/logged-in/inspector/inspector-list/…page.ts` | A | GET `/inspectors?page` (`inspector.service.ts:25`); PATCH `/inspectors/reset-password/{uuid}` (`:65`); DELETE `/inspectors/{uuid}` (`:74`) | ID-12 | |
| `inspector-view/:inspector_uuid` (`:361`) | `inspector-view.page.ts` | A | GET `/inspectors/{uuid}` (`:33`); PATCH reset-password (`:65`) | ID-12 | |
| `inspector-form/:inspector_uuid?` (`:369`) | `inspector-form.page.ts` | A | POST `/inspectors` (`:42`); PATCH `/inspectors/{uuid}` (`:54`) | ID-12 | |
| `candidate-list` (`:85`) | `pages/logged-in/candidate/candidate-list/…page.ts` | A | GET `/candidates/search?page&name&email&phone&civil&expand=store,company` (`candidate.service.ts:28`) | PD-04 | |
| `candidate-review-list` (`:101`) | `candidate-review-list.page.ts` | A | GET `/candidates/search?expand=store,company&by=review&review=0&page` (`candidate.service.ts:132`) | PD-04 | review queue; badge count via GET `/candidates/total-to-review` (`:123`, `app.component.ts` `totalToReview()`) |
| `candidate-view/:id` (`:93`) | `candidate-view.page.ts` | A | GET `/candidates/{id}?expand=…bank,candidateSkills,…` (`:46`); GET `/candidates/transfers/{id}&expand=bank` (`:85`, malformed `&` — see FO-38); GET `/candidates/work-history/{id}?expand=store` (`:151`); PATCH `/candidates/approve/{id}` (`:95`); DELETE `/candidates/{id}` (`:66`); PATCH `/candidates/restore/{id}` (`:76`); PATCH `/candidates/reset-password/{id} {password}` (`:160-161`); POST `/candidates/login/{id}` (`:56`) | PD-03/04, PD-36, PD-37/ID-17, FI-12, WK-05 | shows civil ID number, expiry, civil photos as public S3 links (`candidate-view.page.html:352-395`), bank/IBAN (`:401`) |
| `log-date-list` (`:495`) | `pages/logged-in/candidate/candidate-work-log/log-date-list/…page.ts` | A | GET `/candidate-working-hours/date?page&expand=company,candidate` (`candidate-working-hour.service.ts:20`) | WK-05/06 (staff read) | |
| `log-hour-list` (`:503`) | `log-hour-list.page.ts` | A | GET `/candidate-working-hours/hour?page&expand=store,store.company,candidate` (`:29`) | WK-05 | client totals (FO-27) |
| `can-eval-ques-list` (`:608`) | `pages/logged-in/candidate/evaluation/can-eval-ques-list/…page.ts` | A | GET `/candidate-evaluation/question?page` (`candidate-evaluation.service.ts:28`); DELETE `/candidate-evaluation/question/{uuid}` (`:38`) | RC-14 | |
| `can-eval-ques-dept-form` (`:626`) | `can-eval-ques-dept-form.page.ts` | A | GET `/candidate-evaluation/question/{uuid}` (`:23`); POST `/candidate-evaluation/create-question` (`:47`); PATCH `/candidate-evaluation/update-question/{uuid}` (`:59`) | RC-14 | |
| `can-eval-list` (`:617`) | `can-eval-list.page.ts` | A | GET `/candidate-evaluation/list-candidate-report?page&staffID&candidateID&departmentID` (`:33`); GET `/candidate-evaluation/pdf/{uuid}` (`:82`, pdfget) | RC-14, PD-39 | |
| `evaluation-report-view` (`:653`) | `evaluation-report-view.page.ts` | A | GET `/candidate-evaluation/report/{uuid}?expand=department,staff,candidate,questionAnswer` (`:72`) | RC-14 | |
| `fulltimer-list` (`:563`) | `pages/logged-in/fulltimer/fulltimer-list/…page.ts` | A | GET `/fulltimers?page&name&email&phone&civil` (`fulltimer.service.ts:27`) | RC-16 | |
| `fulltimer-view/:fulltimer_uuid` (`:572`) | `fulltimer-view.page.ts` (+ `components/fulltimer`) | A | GET `/fulltimers/{uuid}?expand=university,area,nationality,country` (`:36`); DELETE `/fulltimers/{uuid}` (`:46`); PATCH `/suggestions/change-status/{uuid} {status}` (`suggestion.service.ts:30-31`, `components/fulltimer/fulltimer.component.ts:53`) | RC-16, RC-06 | |
| `company-list` (`:114`) | `pages/logged-in/company/company-list/…page.ts` | A | GET `/companies?page&name&status&approved_to_hire&staff_id&last_payment_from/to&expand=staff` (`company.service.ts:28`); DELETE `/companies/{id}` (`:103`); GET `/companies/download-list-excel?download=1…` (`:198-199`) | OR-05, OR-11, OR-28/RP-09 | |
| `company-view/:company_id` (`:122`) | `company-view.page.ts` (+ modals `upload-file`, `company-note-form`, `brand-form`, `company-contact-form`, `modal-pop`, `picker/staff`) | A | GET `/companies/{id}?expand=staff,subCompanies,stores,files,brands,requests,parentTransfers…` (`:113`); GET `/companies/download-candidates-excel/{id}?download=1` (`:208-209`); PATCH `/companies/change-status/{id} {status}` (`:218-219`); PATCH `/companies/update-staff/{id} {staff_id}` (`:228-229`); PATCH `/companies/update-followup/{id}` (`:147-151`); PATCH `/companies/update-followup-interval/{id}` (`:160-164`); PATCH `/companies/reset-password/{id}` (`:123-124`) **MISSING-BACKEND** (no rule, no action; button commented out `company-view.page.html:229-236`); GET `/companies/year-report?company_id&year` (`:38-44`); POST `/companies/file-create/{id}` (`:133-139`); PATCH `/companies/file-update/{uuid}` (`:173-179`); DELETE `/companies/remove-file/{uuid}` (`:188-189`); DELETE `/companies/{id}` (`:103`); GET/DELETE `/discounts…` (`discount.service.ts:23,85`); DELETE `/brands/{uuid}` (`brand.service.ts:81`); GET `/company-contacts?expand=…&company_id` (`company-contact.service.ts:24`); DELETE `/company-contacts/{uuid}` (`:137`); GET/DELETE `/notes` (`company-note.service.ts:23,53`) | OR-01/05/07/08/09/10/11, OR-13/16, OR-23, OR-27, OR-28, OR-33/RP-08, FI-16 | `company/login/{id}` (impersonate first contact) exists in service `company.service.ts:238` but is not called by any page |
| `company-form/:company_id?` (`:109`) | `company-form.page.ts` | A | GET `/companies/{id}` (`:113`); POST `/companies` (`:52-70`, `parent` for sub-company, `email`,`password`,`hourly_rate`,`bonus_commission`); PATCH `/companies/{id}` (`:78-94`) | OR-06, OR-07 | commercial terms editable by any admin |
| `company-contact-list` (`:554`) | `company-contact-list.page.ts` | A | GET `/company-contacts?expand=…&page&name&company_id` (`company-contact.service.ts:34`) | OR-13 | |
| `company-contact-view/:contact_uuid[/:company_id]` (`:385`) | `company-contact-view.page.ts` | A | GET `/company-contacts/{uuid}?expand=…` (`:43`); GET `/company-contacts/view-company-contact?contact_uuid&company_id` (`:52`); DELETE `/company-contacts/{uuid}` (`:137`); POST `/company-contacts/login/{uuid}` (`:147`); GET/POST/PATCH/DELETE `/notes` (`note.service.ts:24,48,64,80`) | ID-13, ID-17, OR-13/16, OR-27/CM-16 | note type defaults `'Internal Note'` (`company-contact-view.page.ts:410`) |
| `company-contact-form` (`:346`) | `company-contact-form.page.ts` (modal + route) | — | GET `/company-contacts/is-email-exists?email` (`:61`); PATCH `/company-contacts/add-to-team {company_id,contact_uuid,allow_access,contact_position}` (`:69-79`); POST `/company-contacts` (`:88-104`, plaintext `password`, `allow_access`, emails, phones); PATCH `/company-contacts/{uuid}` (`:112-128`) | ID-13, OR-14/15/17 | no route guard |
| `brand-form` (`:339`) | `brand-form.page.ts` (modal) | — | POST `/brands` (`brand.service.ts:44-54`); PATCH `/brands/{uuid}` (`:62-72`); S3 upload (`aws.service.ts:185`) | OR-23 | no route guard |
| `brand-view/:id` (`:377`) | `brand-view.page.ts` | A | GET `/brands/{uuid}?expand=candidates,stores` (`:26`) | OR-23 | |
| `store-view/:store_id` (`:203`) | `pages/logged-in/store/store-view/…page.ts` | A | GET `/stores/{id}?expand=storeManager,candidates,brand,company,company.brands` (`store.service.ts:35`); GET `/candidates/search?by=store_id&store_id&page` (`candidate.service.ts:114`); POST `/stores/login/{id}` (`store.service.ts:84`) | OR-19, ID-17, PD-06 | `store.service.ts` also defines POST/PATCH/DELETE `/stores` (`:43-75`) — **MISSING-BACKEND** (rules `main.php:222-224` GET/login only), unused by pages |
| `discount-list` (`:889`) | `pages/logged-in/company/discount/discount-list/…page.ts` | A | GET `/discounts?expand=company,store,category&page` (`discount.service.ts:23`); DELETE `/discounts/{uuid}` (`:85`) | FI-16 | |
| `discount-view` (`:894`) | `discount-view.page.ts` | A | GET `/discounts/{uuid}?expand=…` (`:32`) | FI-16 | |
| `discount-form` (`:884`) | `discount-form.page.ts` | A | GET `/discount-categories?page` (`discount-category.service.ts:23`); POST `/discounts` (`:41-55`); PATCH `/discounts/{uuid}` (`:63-76`) | FI-16 | |
| `discount-category-list/-view/:id/-form/:id?` (`:904,:899,:909`) | `pages/logged-in/discount-category/*` | A | GET `/discount-categories?page` (`:23`); GET `/discount-categories/{id}` (`:32`); POST (`:41-49`); PATCH `/discount-categories/{id}` (`:57-64`); DELETE (`:72-74`) | FI-16 | |
| `transfer-list` (`:251`) | `pages/logged-in/transfer/transfer-list/…page.ts` | A | GET `/transfers?page&expand=contract,contract.amount,isSuspicious,profit&company_name&transfer_status&suspicious&start_date&end_date` (`transfer.service.ts:58`); DELETE `/transfers/{id}` (`:344`) | FI-03/05/06 | |
| `transfer-view` (`:283`, id via nav state) | `transfer-view.page.ts` | A | GET `/transfers/{id}?expand=isSuspicious,totalPaid,totalUnpaid,profit,contract…` (`:120`); GET `/transfers/invoices/{id}?expand=company` (`:110`); GET `/transfer-candidates/by-transfer/{id}?page&expand=…` (`candidate.transfer.service.ts:28`); PATCH `/transfers/mark-paid/{id}` (`:73`); PATCH `/transfers/pay-by-wallet/{id} {init_transfer}` (`:130-134`); PATCH `/transfers/payment-received-distributing/{id}` (`:142`); PATCH `/transfers/unlock/{id}` (`:152`); PATCH `/transfers/lock/{id}` (`:325`); PATCH `/transfers/cancel/{id}` (`:334`); POST `/transfers/update-transfer-from-file/{id}` (`:68`); DELETE `/transfers/{id}` (`:344`); GET `/transfers/export/{id}?offset&limit` (`:92-102` excel); GET `/transfers/pdf/{invoice_id}/invoice|receipt` (`:354,:364`) | FI-05, FI-06, FI-07, FI-08, FI-10, FI-18 | status machine in UI (FO-15..19) |
| `suspicious-transfer-list` (`:259`) | `suspicious-transfer-list.page.ts` | A | GET `/transfers/suspicious?page&expand=profit…` (`:81`); GET `/transfers?…` (`:58`); DELETE `/transfers/{id}` (`:344`) | FI-05 | "wrong hourly" list |
| `suspicious-transfer-view` (`:267`) | `suspicious-transfer-view.page.ts` | A | same as transfer-view subset: view (`:120`), invoices (`:110`), by-transfer (`cts:28`), payment-received-distributing (`:142`), unlock (`:152`), lock (`:325`), export (`:92`), pdf (`:354,:364`) | FI-06/07/10 | |
| `transfer-paid` (`:275`, params `excel`,`bank`) | `transfer-paid.page.ts` | A | POST `/transfers/import-excel {excel}` (`:162-166`, AUB); POST `/transfers/import-kfh-excel` (`:185`); POST `/transfers/import-bank-statement-excel` (`:173`); POST `/transfers/import-google-excel` (`:197`); PATCH `/transfers/mark-paid-all {candidates,excel,bank}` (`:223-237`) | FI-08 | bank chosen client-side by string (`transfer-paid.page.ts:56-65`) |
| `import-transfer-form` (`:243`) | `import-transfer-form.page.ts` | A | S3 upload (`aws.service.ts:185`); GET `/transfers/export-google-excel[?only-payable=true]` (`transfer.service.ts:207-214`) | FI-07/08 | navigates to `transfer-paid` with excel URL |
| `payable-candidates` (`:235`) | `payable-candidates.page.ts` (+ `picker/candidate` modal) | A | GET `/transfer-candidates/payable-candidates?expand=candidate&page&searchName&candidateTransferStatus` (`candidate.transfer.service.ts:38`); GET `/transfer-candidates/payable-candidates-stats?…` (`:33`); PATCH `/transfer-candidates/replace/{tc_id} {candidate_id}` (`:80-84`); GET `/transfers/download-text-payment-advice-for-abk?offset&limit` (`transfer.service.ts:282-292`); GET `/transfers/download-payment-advice-for-abk?only-payable&offset&limit` (`:302-316`); GET `/transfers/export-payable-candidates?…` (`:37-50`); GET `/transfers/download-payment-advice?…` (`:263-273`); GET `/transfers/text?…` (`:245-255`) | FI-07, FI-13, RP-09 | client totals (FO-26) |
| `candidate-payment-search` (`:211`) | `candidate-payment-search.page.ts` | A | GET `/transfer-candidates?tc_id&expand=…` (`:57`); GET `/transfer-candidates?transfer_confirmation_id&expand=…` (`:66`) | FI-08 | |
| `candidate-transfer-list` (`:227`) | `candidate-transfer-list.page.ts` | A | GET `/transfer-candidates?tc_id…` (`:57`); PATCH `/transfer-candidates/mark-unpaid-all {transferCandidate}` (`:139-143`); PATCH `/transfer-candidates/mark-paid-all` (`:126-130`) | FI-08 | |
| `candidate-transfer-detail` (`:219`) | `candidate-transfer-detail.page.ts` | A | GET `/transfer-candidates/{tc_id}?expand=…` (`:75`); PATCH `/transfer-candidates/unpaid/{id}` (`:92-93`); PATCH `/transfer-candidates/paid/{id} {transfer_confirmation_id}` (`:114-118`); PATCH `/transfer-candidates/pay-by-wallet/{id} {transfer_confirmation_id, init_transfer}` (`:102-107`) | FI-08, FI-09, FI-18 | |
| `transfer-file-list` (`:323`) | `transfer-file-list.page.ts` | A | GET `/transfer-files?page` (`transfer.file.service.ts:21`); PATCH `/transfer-files/re-schedule/{id}` (`:26`) | FI-08 | |
| `transfer-file-detail` (`:331`) | `transfer-file-detail.page.ts` | A | GET `/transfer-files/{id}?expand=transferFileEntry,transferFileEntry.candidate` (`:31`); GET `/transfer-candidates/by-transfer-file/{id}?page…` (`cts:48`); PATCH re-schedule (`:26`) | FI-08 | |
| `transfer-bank-advice` (`:925`) | `transfer-bank-advice.page.ts` | A | GET `/transfer-bank-advices?page&expand=createdBy` (`transfer-bank-advice.service.ts:32`); DELETE `/transfer-bank-advices/{uuid}` (`:62`); (service also has POST/PATCH `:41-53`, unused by pages) | FI-07 | |
| `bank-transactions` (`:808`) | `pages/logged-in/xero/bank-transactions/…page.ts` | A | GET `/xero?page` (`xero.service.ts:30`) | FI-17 | |
| `bank-transactions-sync` (`:816`) | `bank-transactions-sync.page.ts` | A | GET `/xero/sync?page` looped until `count == 0` (`:34-45`, service `:39`); follows `res.redirect` to Xero OAuth (`:36`) | FI-17 | `xero/view`, `xero/download`, `xero/history` in service (`:21,:48`) but download/history not wired to a page |
| `expense-list` (`:433`) | `pages/logged-in/expense/expense-list/…page.ts` | — | GET `/expenses?page` (`expense.service.ts:35`); DELETE `/expenses/{uuid}` (`:65`) | FI-14 | no route guard |
| `expense-view/:expense_uuid` (`:425`) | `expense-view.page.ts` | — | GET `/expenses/{uuid}` (`:26`) | FI-14 | no route guard |
| `expense-form` (`:429`) | `expense-form.page.ts` | — | POST `/expenses` (`:46`); PATCH `/expenses/{uuid}` (`:56`) | FI-14 | no route guard; limited-access redirect in page (`:42`) |
| `request-list` (`:455`) | `pages/logged-in/request/request-list/…page.ts` | A | GET `/requests?page&name&status&type&staff_id&company_id&expand=requestCreatedBy,company` (`request.service.ts:21`) | RC-01/02 (staff read) | |
| `request-view/:request_uuid` (`:463`) | `request-view.page.ts` | A | GET `/requests/{uuid}?expand=requestSkills,…stories.staff` (`:30`); GET `/notes`, `/invitations`, `/suggestions` lists (`note.service.ts:24`, `invitation.service.ts:21`, `suggestion.service.ts:21`) | RC-02/03/05/07, CM-17 | |
| `story-list` (`:479`) | `pages/logged-in/story/story-list/…page.ts` | A | GET `/story/list?page&name&story_status&position_type&staff_id&company_id&expand=…` (`story.service.ts:22`) | RC-03, CM-17 | `story.service.ts:31` GET `/story/current-story` and `:66` POST `/story/change-story-status` are **MISSING-BACKEND** (no URL rule `main.php:776-795`; `current-story` has no action either) — not called by any admin page |
| `story-view/:story_uuid` (`:487`) | `story-view.page.ts` | A | GET `/story/{uuid}` (`:39`); notes/invitations/suggestions lists | RC-03 | |
| `request-checklist` (`:401`) | `pages/logged-in/requests/request-checklist/request-checklist-list/…page.ts` | A | GET `/request-checklists?page` (`request-checklist.service.ts:23`); DELETE `/request-checklists/{uuid}` (`:62`) | RC-15 | |
| `request-checklist-view/:uuid`, `-form/:uuid?` (`:417,:409`) | view/form pages | A | GET `/request-checklists/{uuid}` (`:32`); POST (`:43`); PATCH `/request-checklists/{uuid}` (`:53`) | RC-15 | |
| `assigned-candidate` (`:447`) | `pages/logged-in/reports/assigned-candidate/…page.ts` | A | GET `/candidates/report-search?page&start&end&company_id&currently_working&assigned=1&expand=candidate,store,company` (`candidate.service.ts:37`) | RP-05, WK-01 (read) | |
| `email-campaign-list` (`:726`) | `pages/logged-in/email-campaign/email-campaign-list/…page.ts` | A | GET `/email-campaigns?expand=emailCampaignFilters&page` (`email-campaign.service.ts:32`); POST `/email-campaigns/status-list {campaignIDs}` polled every 1 s (`:37-38`, page `:63-72`); DELETE `/email-campaigns/{uuid}` (`:75`) | CM-11 | |
| `email-campaign-view/:campaign_uuid` (`:734`) | `email-campaign-view.page.ts` | A | GET `/email-campaigns/{uuid}?expand=emailCampaignFilters` (`:23`); PATCH `/email-campaigns/run/{uuid}` (`:64-65`) | CM-11 | |
| `email-campaign-form` (`:742`) | `email-campaign-form.page.ts` | A | POST `/email-campaigns` (`:50`); PATCH `/email-campaigns/{uuid}` (`:60`) | CM-11 | |
| `campaign-list/-view/:id/-form` (`:824,:829,:834`) | `pages/logged-in/marketing/campaign/*` | A | GET `/campaigns?expand=totalSale&page` (`campaign.service.ts:26`); GET `/campaigns/{uuid}?expand=contactCampaignChartData,candidateCampaignChartData` (`:36`); POST `/campaigns` (`:55-64`); PATCH `/campaigns/{uuid}` (`:72-88`); DELETE `/campaigns/delete/{uuid}` (`:46`) **MISSING-BACKEND** (rule is `DELETE <id>` only, `main.php:545`); PATCH `/campaigns/click/{uuid}` (`:90`) **MISSING-BACKEND** (no rule/action; unused) | CM-18, RP-12 | |
| `webhook-list` (`:689`) | `pages/logged-in/webhook/webhook-list/…page.ts` | A | GET `/webhooks?page` (`webhook.service.ts:26`); DELETE `/webhooks/{id}` (`:94`) | CM-14/OP-08 | |
| `webhook-view/:webhook_id`, `-form/:id?`, `-test/:id` (`:707,:698,:717`) | view/form/test pages | A | GET `/webhooks/{id}` (`:35`); POST `/webhooks {event,endpoint,method}` (`:44-52`); PATCH `/webhooks/{id}` (`:77-85`); POST `/webhooks/test/{id} {endpoint,method,data}` (`:61-69`) | CM-14 | arbitrary URL, no allow-list (CM-F4) |
| `blocked-ip-list`, `blocked-ip-form/:id?` (`:750,:758`) | `pages/logged-in/blocked-ip/*` | A | GET `/blocked-ips?page` (`blocked-ip.service.ts:32`); GET `/blocked-ips/{uuid}` (`:23`); POST `/blocked-ips {ip_address,note}` (`:41-48`); PATCH `/blocked-ips/{uuid}` (`:56-62`); DELETE (`:70-72`) | ID-16 | |
| `setting` (`:680`) | `pages/logged-in/setting/setting.page.ts` | A | GET `/setting` (`setting.service.ts:20`); PATCH `/setting {<key>: value…}` (`:29`) | OP-02 | form built from returned keys (`setting.page.ts:64-71`); backend only persists Mixpanel/Segment keys (`SettingController.php:76-110`) |
| `mail-log-list`, `mail-log-view/:mail_uuid` (`:791,:799`) | `pages/logged-in/mail-log/*` | A | GET `/mail-logs?page&query` (`mail-log.service.ts:20-25`); GET `/mail-logs/stats/{days}` (`:44`); GET `/mail-logs/{uuid}` (`:34`) | CM-21, OP-04 | |
| `voice-mail-list`, `voice-mail-view` (`:914,:919`) | `pages/logged-in/yeaster/voice-mail/*` | A | GET `/yeaster?page&limit` (`yeaster.service.ts:28`); GET `/yeaster/{id}` (`:18`); GET `/yeaster/download/{id}` (`:23`) | CM-15 | |
| `import` (`:442`) | `pages/logged-in/events/import/import.page.ts` | — | S3 upload (`:87`); POST `/events {event, excel}` (`event.service.ts:20-25`) | OP-10 | no route guard; purpose unestablished (OP-F6) |
| `permission-section-list` (`:511`) | `pages/logged-in/permission/permission-section/permission-section-list/…page.ts` | A | GET `/permission-section?expand=permissionSubSections&page` (`permission.service.ts:23-31`); POST `/permission-section {section_name}` (`:47-50`); PATCH `/permission-section/{uuid} {section_name, companies}` (`:70-74`); POST/PATCH/DELETE `/permission-section/sub[/{uuid}]` (`:58-63,:77-81,:103-104`); DELETE `/permission-section/{uuid}` (`:99-100`); GET `/companies?page` (`company.service.ts:28`) | ID-15 | |
| `assign-permission/:type/:user_id` (`:519`) | `assign-permission.page.ts` | A | GET `/permission-section?expand=…` (`:23`); GET `/permission-section/user-permission/{type}/{id}` (`:38-40`); GET `/staff/{id}` (`staff.service.ts:49`); PATCH `/permission-section/set-permission/{id} {permission, type}` (`:84-89`) | ID-15 | type `staff`/`admin` from URL |
| `bank-list/-view/:id/-form/:id?` (`:37,:53,:45`) | `pages/logged-in/bank/*` | A | GET `/banks?page` (`bank.service.ts:35`); GET `/banks/{id}` (`:26`); POST `/banks` (`:44-55`); PATCH `/banks/{id}` (`:63-75`); DELETE (`:83-85`) | OR-25 | |
| `currency-list/-view/:id/-form/:id?` (`:766,:774,:782`) | `pages/logged-in/currency/*` | A | GET `/currencies?page` (`currency.service.ts:35`, also `list(-1)` at app boot `app.component.ts:173`); GET `/currencies/{id}` (`:26`); POST (`:46`); PATCH `/currencies/{id}` (`:55-56`); DELETE (`:65-66`) | OR-25 | |
| `country-list/-view/:id/-form/:id?` (`:131,:139`; form via module) | `pages/logged-in/country/*` | A | GET `/countries?page&query` (`country.service.ts:24-29`); GET `/countries/{id}` (`:33`); POST (`:41-51`); PATCH `/countries/{id}` (`:59-69`); GET `/countries/download-list-excel?download=1` (`:78-79`); GET `/candidates/search?by=country_id…` (`candidate.service.ts:105`); DELETE `/candidates/{id}` (`:66`, from country-view `:146`) | OR-25, RP-09, PD-36 | no country delete |
| `university-list/-view/:id/-form/:id?` (`:291,:299,:307`) | `pages/logged-in/university/*` | A | GET `/universities?page` (`university.service.ts:26`); GET `/universities/{id}` (`:35`); POST (`:44-51`); PATCH (`:59-66`); DELETE (`:74-76`); GET `/universities/download-list-excel?download=1` (`:85-86`); GET `/candidates/search?by=university_id…` (`candidate.service.ts:142`) | OR-26, RP-09 | |
| `major-*`, `degree-*`, `degree-group-*` (`:839-879`) | `pages/logged-in/{major,degree,degree-group}/*` | A | GET/POST/PATCH/DELETE `/majors`, `/degrees?expand=degreeGroup`, `/degree-groups` (`major.service.ts:26-76`, `degree.service.ts:26-80`, `degree-group.service.ts:26-78`) | OR-26 | |
| `tag-list/-view/:id/-form/:id?` (`:61,:77,:69`) | `pages/logged-in/tag/*` | A | GET/POST/PATCH/DELETE `/tags` (`tag.service.ts:26-74`) | OR-25, PD-32 | |
| `**` (`:936`) | redirect `not-found` | — | — | — | |
| (modal) `picker/staff`, `picker/company`, `picker/candidate` | `pages/logged-in/picker/*` | n/a | GET `/staff?page` (`staff.service.ts:28`); GET `/companies?page&name&status&approved_to_hire` (`company.service.ts:28`); GET `/candidates/search?page…` (`candidate.service.ts:28`) | ID-10, OR-05, PD-04 | modals, not routed |
| (modal) `upload-file` | `pages/logged-in/company/upload-file/upload-file.page.ts` | n/a | S3 direct upload (`:108`); POST `/companies/file-create/{id}` (`company.service.ts:133`); PATCH `/companies/file-update/{uuid}` (`:173`) | OR-03 (documents) | |
| (modal) `company-note-form` | `company-note-form.page.ts` | n/a | POST `/notes` (`company-note.service.ts:32`); PATCH `/notes/{uuid}` (`:43`) | OR-27, CM-16 | |
| (component) `components/candidate`, `components/fulltimer` | suggestion cards | n/a | PATCH `/suggestions/change-status/{uuid} {status}` (`suggestion.service.ts:30`) | RC-06 | |
| (component) `components/image-upload` | | n/a | S3 direct upload via `aws.service.ts:185-227`; creds from GET `/aws/config` (`aws.service.ts:49-51`) | OP-05 | |

Service methods with **no backend at c2ce255** (summary): `GET /staff/view-salary/{id}` (action missing), `PATCH /companies/reset-password/{id}` (no rule/action), `POST/PATCH/DELETE /stores` (rules `main.php:222-224` only list/view/login), `GET /story/current-story` and `POST /story/change-story-status` (no rule), `DELETE /campaigns/delete/{id}` and `PATCH /campaigns/click/{id}` (no rule). Only `view-salary` and `campaigns/delete` are reachable from a page (`staff-salary-form.page.ts:66`, `campaign-list.page.ts:125`).

## C. Navigation structure

Single Ionic side menu in `src/app/app.component.html:4-462`, `[disabled]="!authService.isLogin || !this.authService.navEnable"` (`:4`; `navEnable` set per route `data.navDisable`, `auth.service.ts:69-73`, only `company-form` sets `navDisable: true` `app-routing.module.ts:127`). **No menu item is conditioned on role, `admin_limited_access`, or permission sections** (`grep -c "ngIf" app.component.html` finds only the theme icon `:16-17`, the payable badge `:150` and the update alert `:462`). Groups and items:

| Group (`app.component.html`) | Items → route |
|---|---|
| Action Points (`:21-64`) | Summary → `/dashboard`; Payment Summary → `/dashboard-quicks`; Transfers → `transfer-list`; Transfers with wrong hourly → `suspicious-transfer-list` |
| Call center (`:66-79`) | Voice mails → `/voice-mail-list` |
| Marketing (`:81-104`) | Social Campaign → `/campaign-list`; Email Campaign → `/email-campaign-list` |
| Process Payments (`:107-160`) | Bank Advice Files → `transfer-bank-advice`; Payable Candidates (badge `totalPayableCandidate` `:150-152`) → `payable-candidates`; Payment Search → `candidate-payment-search`; Transfer Files → `transfer-file-list` |
| Accounts (`:164-227`) | Admin Accounts → `admin-list`; Companies → `company-list`; Companies Contacts → `company-contact-list`; Staff → `staff-list`; Inspector → `inspector-list`; Candidate → `candidate-list`; Fulltimer → `fulltimer-list` |
| Report (`:229-303`) | Staff Daily Stand-up Answer → `daily-standup-answer-list`; Staff Work Session → `staff-work-session-list`; Staff Expenses → `staff-expense-list`; Staff Leave Request → `staff-leave-list`; Import Excel → `import`; Assigned Candidate → `assigned-candidate`; Request List → `request-list`; Story List → `story-list` |
| Entities (`:305-386`) | Daily Stand-up Question; Office Expenses → `expense-list`; Candidate Tags → `tag-list`; Banks; Universities; Majors; Degrees; Degree Groups; Countries |
| ungrouped (`:388-410`) | Statistics → `/stats`; Request Checklist; Candidate Working Hours → `/log-date-list` |
| Settings (`:413-441`) | Currencies; Blocked IPs → `/blocked-ip-list`; App Settings → `/setting`; Webhooks → `/webhook-list` |
| Discounts (`:443-455`) | Discount Categories |
| Permission (`:457-469`) | Permission Section → `/permission-section-list` |
| Xero (`:471-481`) | Bank Transactions → `/bank-transactions` |
| Security (`:483-493`) | Mail Logs → `/mail-log-list` |
| Personal (`:495-507`) | Logout (`authService.logout()`) |

Not in the menu but reachable by URL: `candidate-review-list`, `can-eval-*`, `evaluation-report-view`, `staff-salary-list`, `staff-salary-register`, `discount-list`, `admin-form`, `inspector-form`, `webhook-test`, `import-salary-form`, `import-transfer-form`, `transfer-paid`, `dashboard-quicks` (linked), `log-hour-list`. Badge sources: `totalToReview()` in `app.component.ts` polls GET `/candidates/total-to-review` and payable stats on `updatePayable$`/`totalCandidateToReview$` (`app.component.ts:203-209`).

## D. Frontend-only behaviour (ADMIN-FO-nn)

Gate count at `c789b17`: **123 active** `admin_limited_access` conditions in page templates (126 raw `.html` occurrences; 3 sit inside HTML comments), plus 15 active component-side redirects in `.ts`. Backend facts used below: the only server-side `admin_limited_access` checks in the whole admin app are `CandidateController.php:372` (delete) and `TransferController.php:2227` (invoice/receipt PDF for transfers not yet received) — `grep -rn admin_limited_access admin/modules/v1/controllers admin/models` returns only those plus the setters `AdminController.php:105,154` and the login echo `AuthController.php:274`. Permission sections are written by `PermissionSectionController.php:243-290` and read by `:357-374`, and nothing in `admin/modules/v1/controllers` reads `PermissionUser` for authorization (confirms SHU-124 F4). `auth.service.ts:562-571` `restrictedAccess()` unconditionally `return true` (the id allow-list is commented out).

| ID | Screen/route | Behaviour | Frontend evidence | Backend check at c2ce255 | Parity | Owning layer | Slice |
|---|---|---|---|---|---|---|---|
| ADMIN-FO-01 | every list/view/footer | Create/Edit/Delete buttons and footers hidden when `authService.admin_limited_access` is truthy (value copied from login response and Preferences, `auth.service.ts:85,220,248`) — 100+ `*ngIf="!authService.admin_limited_access"` across admin, inspector, staff, company, candidate, transfer, reference-data, webhook, blocked-ip, currency, permission pages | 126 raw occurrences in `.html` under `src/app/pages`, of which **3 are inside HTML comments and inert** (`logged-in/company/company-view/company-view.page.html:229`, `logged-in/expense/expense-view/expense-view.page.html:11`, `logged-in/staff/expenses/staff-expense-view/staff-expense-view.page.html:11`), leaving **123 active template gates**; plus 15 active `.ts` redirects (138 active of 141 raw across `src/app/pages`). Examples: `admin-list.page.html:35,50,74`, `staff-list.page.html:131,151,175`, `company-view.page.html:22,201,243,706`, `candidate-view.page.html:9,431`, `webhook-list.page.html:32,46,71`, `blocked-ip-list.page.html:28,38,56`, `permission-section-list.page.html:74` | **Not enforced** for any of these mutations: `AdminController.php:96-235` (create/update/delete/reset), `StaffController.php:295-613`, `InspectorController.php:96-233`, `CompanyController.php:234-693`, `WebhookController.php:112-192`, `BlockedIpController.php:94-161`, `PermissionSectionController.php:84-322`, `CurrencyController.php:115-192` accept any bearer admin. Enforced only for `candidate delete` (`CandidateController.php:372-377`) | ID-11, ID-15 | server | I1 (SHU-124) |
| ADMIN-FO-02 | `*-form` pages | Form pages redirect limited admins away on init (`if (this.authService.admin_limited_access) { navigate back }`) | `daily-standup-question-form.page.ts:42`, `currency-form.page.ts:42`, `bank-form.page.ts:40`, `blocked-ip-form.page.ts:41`, `email-campaign-form.page.ts:46`, `tag-form.page.ts:41`, `expense-form.page.ts:42`, `country-form.page.ts:40` | Not enforced (see FO-01); `BankController.php:96-182`, `EmailCampaignController.php:136-263`, `ExpenseController.php:96-188`, `CountryController.php:105-147` unguarded | ID-11 | server | I1 |
| ADMIN-FO-03 | `transfer-view`, `suspicious-transfer-view` | Limited admin sees invoices/candidate lines only when `transfer_status` is 3 or 4; export excel gated the same way; transfer-file column hidden | `transfer-view.page.html:141-146,325`; `transfer-view.page.ts:457`; `suspicious-transfer-view.page.ts:282`, `.html:104-106,238` | **Partial**: only the PDF path checks (`TransferController.php:2220-2235`, statuses 1/5/10 blocked); `actionView :281`, `actionExport :2110`, `TransferCandidateController::actionByTransfer :208` return everything to limited admins | FI-06, FI-10 | server | F4 (SHU-128) |
| ADMIN-FO-04 | `transfer-view`, `suspicious-transfer-view` | All transfer state buttons (mark paid, delete, cancel, unlock, received-and-distributing, pay by wallet, revert to lock) hidden for limited admins | `transfer-view.page.html:47-86`; `suspicious-transfer-view.page.html:44-54` | **Not enforced**: `actionMarkPaid :224`, `actionCancel :309`, `actionDelete :340`, `actionPaymentReceivedDistributing :370`, `actionUnlock :415`, `actionLock :451`, `actionPayByWallet :1324` have no access check | FI-06, FI-18 | server | F2 |
| ADMIN-FO-05 | `staff-view`, `staff-list`, `components/candidate`, `components/fulltimer` | Salary tab/field and suggestion accept/reject buttons gated on `authService.restrictedAccess()` which always returns `true` | `auth.service.ts:562-571`; `staff-view.page.html:44,74,124,540`; `staff-list.page.html:175`; `candidate.component.html:37-39`; `fulltimer.component.html:20-22` | Not enforced (`StaffController::actionListSalaries :139`, `SuggestionController::actionChangeStatus :96`) — dead gate | ID-10, FI-15, RC-06 | server (drop gate) | I1 / F4 |
| ADMIN-FO-06 | `assign-permission/:type/:user_id`, `permission-section-list` | Admin assigns permission sub-sections to staff/admin; the admin app itself never reads `permissions` to gate anything (the staff list expands `permissions` `staff.service.ts:28` but no template consumes it: `grep -rn "permissions" src/app/pages --include=*.html` → 0 gating uses) | `assign-permission.page.ts:109-115,160`; `permission.service.ts:38-40,84-89` | Server stores (`PermissionSectionController.php:243-290`) and returns (`:357-374`); **never enforced** by any admin controller (SHU-124 F4 confirmed for the admin app) | ID-15 | server (new grant catalogue) | I1 |
| ADMIN-FO-07 | `login`, `login-two-step` | Client decides whether to show the OTP page from `res.token_status == 0` | `login.page.ts:85-89` | Enforced: token created inactive when `enable_two_step_auth` (`AuthController.php:262`), `findIdentityByAccessToken` only resolves active tokens (common identity, see SHU-124 §3.1) | ID-04 | both | I2 |
| ADMIN-FO-08 | route guard | `AuthService.canActivate` only checks that a `loggedInAdmin` token exists in Preferences; no role/permission; 8 logged-in routes have **no guard at all** (`brand-form :339`, `company-contact-form :346`, `expense-view/-form/-list :425-433`, `import :442`, `staff-salary-form :471`, `import-salary-form :475`) | `auth.service.ts:57-99`; `app-routing.module.ts` lines cited | Backend bearer auth on the corresponding endpoints (`BrandController`, `CompanyContactController`, `ExpenseController`, `EventController`, `StaffSalaryController` all `HttpBearerAuth`) so unguarded routes only render empty pages | ID-03 | client-UX-only | — |
| ADMIN-FO-09 | `admin-form` | `email` regex + required, `password` required on create only (`CustomValidator.emailValidator` regex `custom.validator.ts:13`) | `admin-form.page.ts:80-93` | Server: `common/models/Admin.php:49-52` requires name/email, unique+email format, password required on `newAccount` (`AdminController.php:98`) — **match** | ID-11 | both | I1 |
| ADMIN-FO-10 | `staff-form` | required: name, email(regex), password (create), job_title, salary, salary_currency, hours_per_day, week_start_day, work_days | `staff-form.page.ts:109-143` | Server `common/models/Staff.php:198-207`: requires name, job_title, email (+unique/email), password on `newAccount`; salary/hours/week/work_days only typed (`number`/`integer`), **not required** — client stricter | ID-10, FI-15 | both | I1 |
| ADMIN-FO-11 | `company-form` | required: name, common_name_en/ar, bonus_commission, hourly_rate; on new account also password, website, email(regex); sub-company form omits email/password | `company-form.page.ts:111-159` | Server `common/models/Company.php:98-113`: requires name, common names, bonus_commission, currency_code, country_id; email+hourly_rate required on `newAccount`; hourly_rate on `newSubAccount`; `validateHourlyRate :147` (error attached to non-existent attribute, OR-F11). `website` **not required** server-side; `currency_code`/`country_id` required server-side but not client-side | OR-06, OR-07 | both | O5 (SHU-125) |
| ADMIN-FO-12 | `company-contact-form` | email regex, name/position/allow_access required, password required on create; `is-email-exists` lookup switches the form to "add to team" | `company-contact-form.page.ts:92-113,262-282` | Server `common/models/Contact.php:63-73` requires name+email, email format; password optional (`CompanyContactController.php:160-171` sets only if given); `allow_access`/`position` not validated as required (`:226-231`) | ID-13, OR-14/15 | both | O4 / I3 |
| ADMIN-FO-13 | `bank-form`, `currency-form`, `webhook-form`, `blocked-ip-form`, `country-form`, `university-form`, `major-form`, `degree-form`, `degree-group-form`, `discount-form`, `expense-form`, `email-campaign-form`, `request-checklist-form`, `campaign-form`, `tag-form`, `discount-category-form`, `daily-standup-question-form` | `Validators.required` on all displayed fields (147 `Validators.` usages: `grep -rc "Validators\." src/app/pages | awk -F: '{s+=$2} END{print s}'`) | file:line list in `grep -rn "Validators\." src/app/pages` (e.g. `bank-form.page.ts:83-98`, `currency-form.page.ts:85-100`, `webhook-form.page.ts:36-45`, `blocked-ip-form.page.ts:85-91`, `country-form.page.ts:83-94`, `university-form.page.ts:34-41`, `degree-form.page.ts:42-53`) | Server matches for bank (`common/models/Bank.php:42-47`, plus `bank_transfer_type in range` not in client), currency (`Currency.php:43-46` rate required, `sort_order max 100`), webhook (`Webhook.php:41-46`, method not required), major (`Major.php:45-49` + unique), degree (`Degree.php:40-43`, `degree_group_uuid` **not** required server-side), degree group (`DegreeGroup.php:39`, only name_en), country (`Country.php:43` only EN names + unique), discount (`Discount.php:47`), tag (`Tag.php:31`), request checklist (`RequestChecklist.php:39`), discount category (`DiscountCategory.php:37` only name_en). **Server weaker** for: blocked IP (`BlockedIp.php:36-41` — `ip_address` not required, not validated as an IP), university (`University.php:46-48` — no required rule), expense (`Expense.php:45-47` — `amount` integer, title/type only), email campaign (`EmailCampaign.php:55-65` — subject/message not required), daily standup question (`DailyStandupQuestion.php:37-41` — question not required), campaign (`Campaign.php:45-51` — utm fields not required), staff salary (`StaffSalary.php:40-47` — salary not required) | ID-16, OR-25/26, FI-14/15/16, CM-11, CM-14, CM-18, RC-15, WK-21 | server | O8, I6, C4/C5, F4 |
| ADMIN-FO-14 | `blocked-ip-form` | IP address format is not validated anywhere (client only `required`) | `blocked-ip-form.page.ts:85-91` | `common/models/BlockedIp.php:39` string max 45 only | ID-16 | server | I6 |
| ADMIN-FO-15 | `transfer-view` | Transfer state machine rendered from numeric status: Draft(10)→Locked(5)→Payment Sent(1)→Distribution(3)→Complete(4), Cancelled(0). Buttons shown per status: Delete when status ∈ {10,5,0}; Cancel when 10; Unlock when 5 or 1; Received & Distributing when 1 or 5; Pay by wallet when 3 and unpaid>0; Mark as Paid when 3 and unpaid==0; Revert to Locked when 1 | `transfer-view.page.html:47-86`; `_updateTransferStatus` `transfer-view.page.ts:175-201`; status constants only as literals (no enum in `models/transfer.ts:6-36`) | Server validates each transition: `deleteTransfer` allows 10/5 only (`common/models/Transfer.php:1136-1147`) — UI also offers delete on **cancelled (0)**, which the server rejects (`TransferController.php:340-366`); `cancel()` requires 10 (`Transfer.php:1050-1062`); admin `unlock()` allows 5 or 1 (`admin/models/Transfer.php:96-108`); `lock()` requires 1 (`:76-90`, admin-only relock = FI-F9); `paymentReceived()` allows 1 or 5 (`:116-125`). Match except the cancelled-delete case | FI-06 | both (server authoritative) | F2 |
| ADMIN-FO-16 | `transfer-view` | After `mark-paid` success the client sets `transfer_status = 4` locally before reloading | `transfer-view.page.ts:204-210` | Server `Transfer::markTransferCompleteOnCandidatePaid` decides (`TransferController.php:224-229`) — cosmetic only | FI-06 | client-UX-only | F2 |
| ADMIN-FO-17 | `candidate-transfer-detail` | "Mark paid" refuses to call the API when no `transfer_confirmation_id` is entered; unpaid/pay-by-wallet confirmations | `candidate-transfer-detail.page.ts:215-223,99,148` | Server `admin/models/TransferCandidate.php:147-200` rejects already-paid (`:173-178`) but does **not** require a confirmation id (`TransferCandidateController.php:324-332` passes null through); `markUnpaid :74-98` refuses when total paid is zero | FI-08, FI-09 | server | F3 |
| ADMIN-FO-18 | `transfer-paid` | Bank import parser chosen client-side by route param string (`AUB`, `KFH`, `BankStatement`, else Google) and passed back on `mark-paid-all {excel, bank}` | `transfer-paid.page.ts:44-65,134`; `transfer.service.ts:223-237` | Four separate import actions exist (`TransferController.php:494,636,823,1135`); `actionMarkPaidAll :1369` trusts `bank`/`excel` from body | FI-08 | server | F3 |
| ADMIN-FO-19 | `transfer-file-list`, `transfer-file-detail` | Re-schedule offered for files; UI does not check status | `transfer-file-list.page.ts:88`; `transfer-file-detail.page.ts:109` | Server enforces `status == FAILED` (`TransferFileController.php:99-111`) | FI-08 | server | F3 |
| ADMIN-FO-20 | `staff-expense-view` | Expense status machine in UI: Approve(1)/Declined(3) only when status falsy; "Mark as Reimbursed"(2) only when status==1 | `staff-expense-view.page.html:101-103`; `.page.ts:100-125` | Server `StaffExpensesController.php:179-205` sets any integer from body; `common/models/StaffExpenses.php:49-50` types `status` integer only — **no transition/range check** | FI-14 | server | F4 (or drop per D-FI4) |
| ADMIN-FO-21 | `staff-leave-list` | Approve(1)/Declined(2) only when `!leave.status` | `staff-leave-list.page.html:27-28`; `.page.ts:150-165` | `StaffLeaveController.php:128-142` saves any body value; `common/models/StaffLeave.php:42-47` no range rule — **not enforced** | WK-20 | server | W-internal (D-WK4) |
| ADMIN-FO-22 | `components/candidate`, `components/fulltimer` | Suggestion status buttons (Suggested 1 / Rejected 2 / Accepted 3) hidden only when equal to current | `candidate.component.html:37-39`; `fulltimer.component.html:20-22`; `suggestion.service.ts:29-33` | `SuggestionController.php:96-115` saves body status; `common/models/Suggestion.php:54-56` validates range only, **no transition check** | RC-06 | server | R4 (SHU-127) |
| ADMIN-FO-23 | `company-view` | Company status shown as computed `companyStatus`, and the toggle writes `company_status_override` 1/0 ("override above status to active when not active") | `company-view.page.html:237-246`; `company-view.page.ts:1104-1113`; `company.service.ts:217-219` | `CompanyController::actionChangeStatus :617-645` sets `company_status_override` from body under scenario `updateStatus` (`Company.php:207`); employer projection ignores the override (OR-F2) | OR-08 | server (one derivation) | O1/O5 |
| ADMIN-FO-24 | `company-view` | Follow-up flag/interval and account-manager (`update-staff`) picked in UI via staff picker; follow-up toggle hidden for sub-companies (`!company.parent_company_id`) | `company-view.page.html:195-246`; `.page.ts:1066-1072,1214-1225,523-551`; `company.service.ts:146-164,227-229` | Server accepts any staff_id/interval with scenario whitelists (`CompanyController.php:655-775`, `Company.php:205-208`); no parent/child check | OR-09, OR-10 | server | O5 (or Attio per D-OR3) |
| ADMIN-FO-25 | `company-view`, `company-list` | Excel export of company list and of a company's candidates triggered from UI with no audit prompt | `company-list.page.ts:358`; `company-view.page.ts:172`; `company.service.ts:197-209` | `CompanyController.php:829,933` — unaudited (OR-F5, RP-F3) | OR-28, RP-09 | server | O9 / P4 |
| ADMIN-FO-26 | `payable-candidates` | Totals (unpaid, payable, missing-bank-info, incomplete-profile) and `totalPayableCandidate` computed client-side by iterating the current page's `unPaidTransferCandidates`; a line is "missing bank info" if no `bank_id`/`transfer_benef_iban`/`transfer_benef_name` | `payable-candidates.page.ts:329-355` | Server has its own stats endpoint (`TransferCandidateController::actionPayableCandidatesStats :72`) — two sources of truth; page-scoped sums are wrong across pages | FI-07, FI-13 | server | F3 |
| ADMIN-FO-27 | `log-hour-list`, `staff-work-session-list` | Total hours / total minutes summed client-side over loaded rows | `log-hour-list.page.ts:104`; `staff-work-session-list.page.ts:242` | Server returns per-row `total_time`/`total_minutes` only (`CandidateWorkingHourController.php:102`, `StaffWorkSessionController.php:72`) | WK-05, WK-15, WK-19 | server (derived read model) | W3/W6 |
| ADMIN-FO-28 | `transfer-view` (+ suspicious, file-detail) | Per-line money helpers `totalCompanyPaysForCandidateWithoutBonus = company_total - bonus` computed in the client (the rate×hours version is commented out) | `transfer-view.page.ts:500-525`; `transfer-file-detail.page.ts:170-190`; `suspicious-transfer-view.page.ts:330-350` | Server computes `company_total`/`candidate_total` (`common/models/TransferCandidate.php:87-98`, FI-F1) | FI-05 | server | F2 |
| ADMIN-FO-29 | `candidate-view` | Approve button only when `!approved && !deleted`; Delete when `!deleted`; Restore when `deleted`; login/reset footer hidden when deleted or limited | `candidate-view.page.html:10-19,431` | Server: approve sets `approved=1` unconditionally (`CandidateController.php:249-283`); delete checks limited access and already-deleted (`:370-385`); restore unconditional (`:290-323`); reset-password generates a 5-char password if none given and emails it (`:419-435`, ID F5) | PD-36 | both | S6/S9 (SHU-123) |
| ADMIN-FO-30 | `candidate-view`, `staff-view`, `company-contact-view`, `store-view` | Impersonation: client POSTs `login/{id}` and opens the returned `redirect` URL (`<app>?auth_key=…`) in a new tab; no reason/audit captured in UI | `candidate-view.page.ts:376-391`; `staff-view.page.ts:844-859`; `company-contact-view.page.ts:414-429`; `store-view.page.ts:139-154` | Server regenerates the target's auth key and returns it in the URL (`CandidateController.php:223-247`, `StaffController.php:79-100`, `CompanyContactController.php:78-100`, `StoreController.php:103-130`); no audit row (ID F6); limited admins not blocked | PD-37, ID-17 | server (audited act-as or drop, D2) | I7 |
| ADMIN-FO-31 | `email-campaign-list` | Polls `POST /email-campaigns/status-list` every **1 s** while any campaign is status 1 or 3, filtering the id list client-side | `email-campaign-list.page.ts:63-90`; `email-campaign.service.ts:36-44` | Server just returns status/progress (`EmailCampaignController.php:84`); progress semantics per CM-F5 | CM-11 | client-UX-only (rate) | C4 |
| ADMIN-FO-32 | `email-campaign-view` | "Start" sets status READY via `run/{id}`; UI does not check current status | `email-campaign-view.page.ts:85`; `email-campaign.service.ts:63-65` | `EmailCampaignController::actionRun :114-129` sets `STATUS_READY` unconditionally (re-runs a completed/in-progress campaign) | CM-11 | server | C4 |
| ADMIN-FO-33 | `webhook-form`, `webhook-test` | Endpoint URL free text; `method` required client-side; test posts arbitrary `data` JSON | `webhook-form.page.ts:36-45`; `webhook-test.page.ts:36-46,78` | `WebhookController::actionCreate :112-150` / `actionTest :97-107` — no allow-list, no scheme check, `method` not required (`Webhook.php:41-46`); CM-F4 | CM-14, OP-08 | server | C5 |
| ADMIN-FO-34 | `setting` | Form controls generated from whatever `GET /setting` returns; password-type toggling per key; `isProduction` flag from environment | `setting.page.ts:40-71,110-116` | `SettingController::actionUpdate :76-110` reads only the Mixpanel/Segment keys by name — other keys silently ignored | OP-02 | server (typed config) | X1 (SHU-139) |
| ADMIN-FO-35 | `dashboard` | "Clear cache" calls `GET /statistics/clear-cache` then `window.location.reload()` | `dashboard.page.ts:256-261`; `statistics.service.ts:32-34` | `StatisticController::actionClearCache :85-87` flushes the whole app cache for any bearer admin (RP-F4) | RP-10, OP-13 | server (discard) | — |
| ADMIN-FO-36 | `components/image-upload`, `brand-form`, `staff-form`, `upload-file`, `import*` | Direct browser S3 upload with `ACL: 'public-read'` to bucket `studenthub-public-anyone-can-upload-24hr-expiry` using static key/secret fetched from `GET /aws/config`; 18 MB client-side size cap; region default `eu-west-2` | `aws.service.ts:21-31,49-73,185-227`; `permanentBucketUrl`/`cloudinaryUrl` in `environment.prod.ts:5,7` and 37 template usages (`grep -rn permanentBucketUrl src/app --include=*.html`) | `AwsController.php:56-66` returns `aws_temp_access_key_id`/`secret` from params **with authentication removed** (`:15-35`) — OP-F1/SHU-134; no server-side size/type check | OP-05, PD-22..24 | server (short-lived scoped creds) | S4 / X1 |
| ADMIN-FO-37 | `login`, `login-two-step`, `app.module`, `index.html` | Client-held keys: reCAPTCHA v3 site key (`index.html:30`, `login.page.ts:67`, `login-two-step.page.ts:44`); Auth0 domain+clientId (`app.module.ts:85-86`); Google OAuth clientId (`app.component.ts:59`); Segment write key (`index.html:33-34`); Sentry DSN (`environment.prod.ts:10`); Cloudinary base (`:5`) | as cited | reCAPTCHA verified server-side via `g-recaptcha-response` header (`AuthController.php:135` per SHU-124 §3.1); others are public identifiers by design | ID-03/05, OP-07/09 | client (public ids) | X1 register |
| ADMIN-FO-38 | `candidate-view` (salary tab) | Transfers URL built as `/candidates/transfers/{id}&expand=bank` (ampersand instead of `?`) | `candidate.service.ts:85` | Route `GET transfers/<id>` (`main.php:242`) captures `"{id}&expand=bank"` as `$id`; `actionTransfers :330` casts — works by accident, `expand` never applied | FI-12 | client fix | — |
| ADMIN-FO-39 | `staff-salary-form` | Edit mode loads `GET /staff/view-salary/{uuid}` which has a URL rule but **no controller action** → 404 → global `error404$` navigates to `not-found` | `staff-salary-form.page.ts:66`; `staff.service.ts:68-71`; `authhttp.service.ts:301-304` | `StaffController.php` has no `actionViewSalary` (`grep -n "public function action" StaffController.php`) | FI-15 | server | F4 / D-FI4 |
| ADMIN-FO-40 | `login-two-step/:token` | Route guarded by `AuthService` (`app-routing.module.ts:931-935`) although the OTP step happens before a token is stored; guard resolves false and redirects to `login` unless a previous session's `loggedInAdmin` remains in Preferences | `app-routing.module.ts:931`; `auth.service.ts:76-93` | n/a | ID-04 | client fix | I2 |
| ADMIN-FO-41 | global | Session/state persistence: bearer token, id, name, email, `admin_limited_access`, `currency_pref`, `theme` in Capacitor Preferences (web: localStorage); `Preferences.clear()` on logout; 401 → logout; 0/504 or `!navigator.onLine` → `no-internet` page; 500 → `server-error`; 404 → `not-found`; retries: GET/PATCH 1 retry with backoff, POST/DELETE 0, `mark-paid-all` custom (`transfer.service.ts:223-237`); Angular service worker with `freshness` strategy, `maxAge 0` for API (`ngsw-config.json:6-30`) | `auth.service.ts:243-249,158-176`; `storage.service.ts`; `authhttp.service.ts:150-160,280-321`; `util/genericRetryStrategy.ts:4-31` | Token validity checked server-side per request (`HttpBearerAuth`) | ID-08, ID-09 | client-UX-only | I2 |
| ADMIN-FO-42 | `login` | Client counts failed attempts (>2 → "contact us" alert); no lockout | `login.page.ts:31-33,100-117` | Server has no login throttle beyond reCAPTCHA (ID F7) | ID-03 | server (edge) | I6 |
| ADMIN-FO-43 | `transfer-paid`, `payable-candidates` | `offset`/`limit` for bank-file/advice exports and `only-payable` are user-chosen in the UI and appended to export URLs | `payable-candidates.page.ts:240-300`; `transfer.service.ts:36-50,244-316` | Exports unaudited (`TransferController.php:1449,1591,1838,1971,2035`); RP-F3 | FI-07, RP-09 | server | F3 / P4 |
| ADMIN-FO-44 | `bank-transactions-sync` | Client drives Xero sync page-by-page in a loop until `count == 0`; follows `res.redirect` (OAuth) by setting `location` | `bank-transactions-sync.page.ts:31-50` | `XeroController::actionSync :210` per page; `auth`/`callback` unauthenticated (`XeroController.php:51`) | FI-17 | server (job) | deferred (SHU-46) |
| ADMIN-FO-45 | `staff-form`, `staff-view` | Gmail username/password captured in the form and displayed back from `staff_gmail_password` | `staff-form.page.ts:112-113,135-136,159-160` | Stored reversibly and decrypted into responses (`StaffController.php:305`, `admin/models/Staff.php:49-52`; ID F3) | ID-10 | server (remove) | I5 |
| ADMIN-FO-46 | `company-contact-view`, `company-view` notes | Note `type` defaults to `'Internal Note'` and is free text; component shows `note.note_type` | `company-contact-view.page.ts:294,348,410`; `components/note/note.component.html:5` | `NoteController.php:129-190` stores as given; no vocabulary | OR-27, CM-16 | server (typed) | O9 / C-notes |
| ADMIN-FO-47 | `company-view` | `companyContacts` list filtered client-side after delete; contact delete from company view is a hard delete | `company-view.page.ts:773-779`; `company-contact.service.ts:136-138` | `CompanyContactController::actionDelete :383` hard-deletes (OR-F4) | OR-16 | server (soft delete) | O6 |
| ADMIN-FO-48 | `candidate-view` | Civil ID number, expiry, front/back photo links (public S3 URLs), bank account/IBAN rendered to every admin incl. limited | `candidate-view.page.html:350-420`; links `aws.permanentBucketUrl + 'photos/' + …` `:381,:393` | `CandidateController::actionView :342` returns the full model to any admin; objects are public-read (SHU-54) | PD-24, PD-27 | server (scoped projection + private objects) | S4/S6 |

## E. Journeys with no corresponding parity row

| Journey | Route/screen | Endpoint(s) | Suggested cluster | Why it's missing |
|---|---|---|---|---|
| Candidate evaluation question bank per department and evaluation report PDF | `can-eval-ques-list`, `can-eval-ques-dept-form`, `can-eval-list`, `evaluation-report-view` (`app-routing.module.ts:608-660`) | `/candidate-evaluation/*` (10 actions, `CandidateEvaluationController.php:76-258`) | RC (RC-14 covers "evaluate a candidate, versioned notes" but not the admin-managed question catalogue / department assignment / PDF) | Inventory RC-14 is scoped to `request_interview` evaluation; the `candidate_evaluation` question/report structure is a separate feature |
| Transfer bank-advice file registry | `transfer-bank-advice` (`:925`) | `/transfer-bank-advices` CRUD (`TransferBankAdviceController.php:69-164`) | FI (near FI-07) | FI-07 covers "export a bank transfer file"; the serial-numbered advice register is not named |
| Suspicious-transfer review ("wrong hourly") | `suspicious-transfer-list`, `suspicious-transfer-view` (`:259-273`) | `GET /transfers/suspicious` (`TransferController.php:2268`) | FI (FI-05) | FI-05 mentions adjusting lines; the suspicious flag/list (`isSuspicious` expand) is a distinct review queue |
| Replace the payee candidate on a transfer line | `payable-candidates` replace modal (`payable-candidates.page.ts:360-400`) | `PATCH /transfer-candidates/replace/{id}` (`TransferCandidateController.php:257-313`) | FI (FI-05/FI-13) | Not listed; it rewrites `candidate_id`, bank and IBAN on a payable line |
| Payment search by tc_id / confirmation id | `candidate-payment-search` (`:211`) | `GET /transfer-candidates?tc_id|transfer_confirmation_id` | FI (FI-08) | Reconciliation lookup not named as a journey |
| Staff daily stand-up questions/answers admin | `daily-standup-*` (`:527-553`) | `/daily-standup-questions`, `/daily-standup-answers` | WK-21 exists (generic) | WK-21 is one row for the whole feature; the admin question catalogue and inactive-staff report are not itemised |
| Voice-mail (Yeaster PBX) browsing | `voice-mail-list/-view` (`:914-923`) | `/yeaster`, `/yeaster/{id}`, `/yeaster/download/{id}` | CM-15 (call recordings) | CM-15 exists but the admin-app screen is the only consumer; noted for SHU-138 |
| Marketing UTM campaigns with investment/revenue | `campaign-*` (`:824-838`) | `/campaigns` (`CampaignController.php`) | CM-18 / RP-12 | Rows exist; the admin `delete/{id}` route is broken (MISSING-BACKEND) |
| Assigned-candidate report (currently working, by company, date range) | `assigned-candidate` (`:447`) | `GET /candidates/report-search` (`CandidateController.php:151`) | RP (RP-05) / WK-01 | Not itemised as a report |
| Sub-company creation with inherited contact | `company-form` with `parent` | `POST /companies {parent}` (`CompanyController.php:248-252`) | OR-06 | OR-06 names "sub-organization"; covered, listed here because the client form variant differs (no email/password) |
| Import Excel of analytics events | `import` (`:442`) | `POST /events` (`EventController.php:74`) | OP-10 | OP-10 exists but purpose unresolved (OP-F6) |

## F. Role-specific flows (grant that would replace them)

| Admin-app flow | Replacement grant in the one-app model |
|---|---|
| Full admin: every menu item, all CRUD on accounts, reference data, finance, settings | `admin:*` (I1 grant catalogue) split into `admin:identity` (admin/staff/inspector accounts, permission sections, blocked IPs), `admin:orgs` (companies, contacts, brands, reference data), `admin:reference-data` |
| Limited admin (`admin_limited_access=1`): read everything, no create/edit/delete, transfer money views only after status 3/4, no candidate delete | `admin:read-only` plus `finance:read-settled` — today this is 123 active template gates (126 raw, 3 inert inside HTML comments) and 2 server checks |
| Finance operator: transfers, lock/unlock/cancel, mark paid, bank files, imports, payable candidates, invoices/receipts, wallet, Xero | `staff:finance` (F2/F3/F4), with lock-state transitions as server-side capabilities |
| Candidate approver/reviewer (review queue, approve, delete/restore, reset password) | `staff:candidate-admin` (S6/S9) |
| Impersonation of candidate/staff/contact/store manager | `admin:act-as` audited (I7) or removed (D2) |
| Permission section administration | replaced by the grant catalogue admin UI (I1); nothing to port |
| Marketing/email campaigns, webhooks, settings, cache flush, mail log | `admin:comms` (C4/C5) and `admin:platform` (X1/X2); cache flush discarded |
| Internal HR/finance (staff work sessions, leave, stand-ups, salaries, office/staff expenses) | out of scope per D-WK4 / D-FI4; otherwise `staff:hr-admin` |
| Inspector account CRUD | removed with the `status` app per D-RP1, else `admin:identity` |

## G. Not established

- Whether `admin_limited_access` admins exist in production and what they are used for (only 2 server checks; the UI implies a "view-only accountant" role).
- Whether `GET /aws/config` is reachable from the public internet at `admin.api.studenthub.co` (authenticator removed at `AwsController.php:15`; needs SHU-141-style probe).
- Whether the `staff/view-salary` 404 and `campaigns/delete` 404 are hit in practice (Sentry would show `error404$` navigations); both are dead paths at c2ce255.
- Real values behind `restrictedAccess()` (commented allow-list ids 1, 7, 10 — `auth.service.ts:566-569`) and whether anyone relies on it.
- Which `/setting` keys the UI shows in production (form is data-driven; backend persists only Mixpanel/Segment keys).
- Whether the Capacitor mobile build (`co.studenthub.admin`, Google/Auth0 native login) is actually distributed; no store metadata in repo.
- The `transfer-view` route takes the transfer via router state, not a URL param (`app-routing.module.ts:283`); deep links therefore cannot open a transfer — confirm intended.

## Summary — highest-value frontend-only findings

1. Limited-access admin is a UI convention: 123 active `admin_limited_access` template gates (126 raw, 3 inert inside HTML comments) vs 2 server checks (`CandidateController.php:372`, `TransferController.php:2227`); every other create/update/delete/status/transfer action is open to a limited bearer token (FO-01..04).
2. Permission sections are administered from the admin app (`assign-permission`, `permission-section-list`) but never consulted by the admin app or any admin controller — SHU-124 F4 confirmed end-to-end (FO-06).
3. `restrictedAccess()` always returns `true` (`auth.service.ts:562-571`); the salary/suggestion gates that depend on it are dead (FO-05).
4. Transfer status machine is mirrored in the UI with numeric literals; server validates transitions, but the UI offers Delete on cancelled transfers which the server rejects, and role-dependent relock/unlock (FI-F9) is only visible via hidden buttons (FO-15).
5. Staff-expense, staff-leave and suggestion status changes are client-decided with no server transition or range check (FO-20..22).
6. Direct-to-S3 uploads use static credentials from an **unauthenticated** `GET /aws/config`, uploading `public-read` objects; civil-ID photos are rendered as public bucket links (FO-36, FO-48).
7. Two live dead-ends: `GET /staff/view-salary/{id}` (rule, no action) and `DELETE /campaigns/delete/{id}` (no rule); plus unused service methods for `/stores` CRUD, `/story/change-story-status`, `/companies/reset-password` (section B).
8. Money totals on `payable-candidates`, `log-hour-list`, `staff-work-session-list` and per-line bonus math are computed client-side over the loaded page (FO-26..28).
9. Client validation is stricter than the server for blocked IPs (no IP format anywhere), universities, expenses, email campaigns, standup questions, UTM campaigns and staff salaries — server `rules()` lack `required`/format checks (FO-13/14).
10. Impersonation, exports, cache flush and webhook registration are one click with no reason capture or audit on either side (FO-30, FO-25/43, FO-35, FO-33).
